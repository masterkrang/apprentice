package view
{
	
	/*this should be a panel that holds all of the settings
		the application will read from this file on init, and possibly allow the user
		to change settings at runtime. should also hold all default values */
	
	public class SettingsPanel
	{
		
		
		public function SettingsPanel()
		{
		}
	}
}